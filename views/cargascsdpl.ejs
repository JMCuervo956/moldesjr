<!DOCTYPE html>
<html lang="es">
    <style>
        table {
          font-size: 16px; /* Tamaño de fuente más pequeño */
          line-height: 0.6; /* Reduce el espacio entre líneas */
          border-collapse: collapse; /* Para evitar doble borde */
          width: 100%;
        }
        th, td {
          padding: 5px; /* Espacio interno en las celdas */
          border: 1px solid #190186; /* Bordes de las celdas */
        }
        th {
            text-align: left;
            }
        td {
            text-align: left;
            }
        /* Definir el ancho de cada columna */
        th, td {
            width: 30%; /* Ancho relativo para las columnas */
            }        
        /* Definir el ancho de columnas individuales */
        th:nth-child(1), td:nth-child(1) {
        width: 10%; /* Ajusta el ancho de la primera columna */
        }
        th:nth-child(2), td:nth-child(2) {
        width: 10%; /* Ajusta el ancho de la segunda columna */
        }
        th:nth-child(3), td:nth-child(3) {
        width: 80%; /* Ajusta el ancho de la tercera columna */
        }
    </style>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tabla de Datos desde MySQL</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <%- include('partials/head') %>
</head>
<body>
  <%- include('partials/navigation') %>  
  <h4 style="color: rgb(2, 150, 128);">Propiedades NO Cargadas :  <%= recordCount %> Ya Existente o Duplicadas</h4>
  <a href="/cargausuarios" class="btn btn-danger btn-sm">Regresar</a>

    <div class="container mt-5">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Propiedad</th>
                    <th>Usuario</th>
                    <th>Nombre</th>
                </tr>
            </thead>
            <tbody>
                <!-- Aquí iteramos sobre los datos y los mostramos en la tabla -->
                <% data.forEach(row => { %>
                    <tr>
                        <td><%= row.numprop %></td>
                        <td><%= row.user %></td>
                        <td><%= row.name %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
      </div>
      <p><strong>Total de registros: </strong><%= recordCount %> Ya existentes o Duplicadas</p>
  </body>
</html>
